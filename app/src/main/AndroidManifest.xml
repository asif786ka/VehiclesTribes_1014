<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shirts.com.roomormshirts">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="shirts.com.roomormshirts.AAApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="shirts.com.roomormshirts.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="shirts.com.roomormshirts.ui.detail.ShirtDetailActivity"
            android:theme="@style/DetailTheme" />
        <activity
            android:name="shirts.com.roomormshirts.ui.cart.CartActivity"
            android:theme="@style/DetailTheme" />
    </application>

</manifest>